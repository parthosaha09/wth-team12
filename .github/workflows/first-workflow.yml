
on:
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'
        required: true
        default: 'warning'
        type: choice
        options:
        - info
        - warning
        - debug

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step 1
        run: echo "step 1"
      - name : step 2
        run: echo "step 2"